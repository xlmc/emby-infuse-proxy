# 更新说明

本仓库以可回滚的源码为准。历史源码缺失的版本会明确标注，避免将说明文件误认为可恢复的版本包。

## v24（2026-08-23，当前稳定版）

- 修复 `cf_deploy.py`：部署前从当前活动 Worker 版本读取 binding 类型，并通过 `keep_bindings` 保留 KV、认证和路由配置。
- 旧 settings 接口不会返回版本绑定；部署脚本无法读取绑定时会停止上传，避免再次将 Worker 配置清空。
- 此版不改变 Worker 运行时逻辑，生产已恢复到含完整绑定的稳定 Worker 版本。

## v23（2026-08-23，已被 v24 取代）

- 紧急撤回 v22 对 Infuse 虚拟媒体播放改写的直连变更，恢复经验证可用的 v21 兼容链路。
- v22 会影响部分现有 Infuse 播放地址的解析，不能作为稳定版本部署。

## v22（2026-08-23，已撤回）

- 修复 Infuse 播放 `vl-*` / `ve-*` 虚拟媒体时，播放地址兼容改写会强制回退到 Worker 中转的问题。
- `mainVideoStreamMode: "direct"` 现在仍保留 `/emya/video` 的兼容改写，但媒体请求会返回客户端可跟随的 `307`，不再由 Worker 传输视频字节。
- 仅将可解析的 Emby 登录令牌转换为上游 URL 查询参数；Cookie 或未知自定义鉴权仍会拒绝直连，避免鉴权泄漏或静默降级。

## v21（2026-08-23，已被 v23 取代）

- 修复 Infuse 电影列表元数据补全的上游地址读取：优先读取响应上下文，缺失时从请求上下文兜底。
- 电影封面、演员、类型、标签、制片厂和媒体信息恢复稳定补全。
- 保留虚拟 ID 的单项详情并发策略与 `meta4` 缓存键，避免批量接口造成空数据或占位封面。

## v20（2026-08-23，已被 v21 取代）

- 放弃对 Emos 虚拟电影 ID 不兼容的批量 `Ids=` 元数据接口。
- 恢复最多 6 路的单项详情请求，并将总处理窗口恢复为 6.5 秒。
- 增加 `meta4` 缓存键，修复已获取详情无法形成稳定缓存的问题。

## v19（2026-08-23，已被 v20 取代）

- 为批量元数据请求增加单项详情兜底，并避免缓存空结果与虚拟封面。
- 该版单项超时过短，慢上游下仍可能无法补全，故不建议部署。

## v18（2026-08-23，已被 v19/v20 取代）

- 首次引入批量元数据补全，扩展演员、类型、标签、制片厂和媒体信息。
- Emos 对 `vl-*` 虚拟 ID 的批量响应可能为空、部分缺失或仍为占位封面；该版会造成封面、演员和标签同时缺失，不建议部署。

## v17（2026-08-22）

- 修复剧集行误查与 BGM 评分错配。
- 增加名称相关性校验和 `noRating` 标记，避免剧集被写入错误评分。

## v16（2026-08-22）

- 海报与背景图改用原画分辨率 `original`。
- 部署脚本改为从脚本同目录读取源码。

## v15（2026-08-22）

- 缓存命名空间升级，等效全量清理旧缓存。
- 收录 v4 原始 Worker 备份：`worker-original-v4-backup.js`。

## v14（2026-08-22）

- Infuse 兼容的 Emby 虚拟 ID 源反代基础版本。
- 整合 v6–v13 的播放、封面、演员、元数据、评分、继续观看与缓存兼容性修复。
- 修正响应缓存边界，避免将不适合长期缓存的列表结果错误复用。

## v13（历史补丁）

- 首次引入 Infuse JSON 响应缓存，降低重复同步的上游请求。
- 此版部分列表缓存范围过宽；边界策略已由 v14 收紧。

## v12（历史补丁）

- 为继续观看（Resume）行补充运行时长等必要媒体信息。
- 改善 Infuse 对未完整返回详情的继续观看条目的识别。

## v11（历史补丁）

- 为列表详情补全设置处理时间预算与并发控制。
- 避免首次同步时大量详情查询长期阻塞列表响应。

## v10（历史补丁）

- 向列表元数据注入 Bangumi `CommunityRating`，供 Infuse 显示评分徽标。
- 使用年份 ±1、评分人数不少于 50 的守卫，未命中时保持无评分。

## v9（历史补丁）

- 修复上游人物共用 `pn-3` ID 导致 Infuse 只显示一位演员的问题。
- 通过唯一人物 ID 规范化保留完整演员表。
- 清理敏感直链字段后传递更多媒体流信息，以支持 4K、杜比视界等标签。

## v8（历史补丁）

- 为瘦电影列表行补充详情接口中的简介、演员、时长、日期、分级和媒体信息。
- 引入详情元数据缓存与封面回退。

## v7（历史补丁）

- 规范化 `People[].PrimaryImageTag`，修复演员头像不渲染。

## v6（历史补丁）

- 电影流路由兼容 `vl-*` 虚拟 ID。
- 电影列表也纳入封面、背景和虚拟封面 tag 修复。
- 仅为剧集设置 `SeriesPrimaryImageTag`，避免电影行被错误标记。

## v5（历史基线）

- v6–v10 补丁均以此版为基线。
- 包含图片镜像链与封面缓存的初版实现。
- 当前工作区仅保留针对 v5 的补丁脚本，未保留完整 v5 源码快照。

## v4（历史备份）

- 可用源码：`worker-original-v4-backup.js`。
- 原始提交与逐项变更说明未随仓库保留。

## v1–v3（历史源码缺失）

- 当前本地仓库、远端分支、标签和 Git 对象库均无这些版本的源码或提交。
- v6–v10 的补丁说明已由原始补丁脚本恢复；v11–v14 的说明已由完整旧会话记录恢复。
- 如补充 v5 基线即可重建 v6–v10 的精确源码快照；如补充其他版本包，可补建精确标签和逐版可回滚源码。
- 如补充其他版本包，可在此基础上补建精确标签和逐版可回滚源码。
